const Favorites: React.FC<Props> = ({navigation, route}) => {
  return (
    <SafeAreaView>
      <Text>Image</Text>
    </SafeAreaView>
  );
};

//make this component available to the app
export default Home;
